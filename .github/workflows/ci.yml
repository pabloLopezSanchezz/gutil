name: CI

on:
  workflow_call:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
          cache: false
      - name: Verify formatting
        run: |
          gofmt -w .
          git diff --exit-code
      - run: go vet ./...
      - run: go test ./...
      - name: Run Windows installer end to end
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          $env:GUTIL_INSTALL_DIR = Join-Path $env:RUNNER_TEMP 'gutil-install-test'
          & ./scripts/install.ps1
          gutil version
      - name: Run race detector
        if: runner.os != 'Windows'
        run: go test -race ./...
